# UI（Iced）TODO

## フェーズ 1: 骨組み

- [x] タイトルが `Decision Pack UI (Iced)` のウィンドウを作る。
- [x] 上段入力列として `Input JSON Path` + `Browse` + `Load` を置く。
- [x] 下部にステータスバー（`準備完了 / 読込済み / エラー`）を置く。

## フェーズ 2: データモデルと解析

- [x] UI で使う `simulation_report_v0.1` の Rust 構造体を定義する。
- [x] `Load` 押下時に入力パスの JSON を解析する。
- [x] ファイル未存在や JSON 解析失敗時に分かりやすいエラーを表示する。

## フェーズ 3: メイン表示

- [x] シナリオ名と生成時刻を表示する。
- [x] KPI カードとして `min_cash` `total_stockout_qty` `stockout_rate` を表示する。
- [x] 重要度・日付・メッセージ・任意の item id を含むアラート一覧を表示する。

## フェーズ 4: 成果物操作

- [x] `cash_balance.png` `daily_stockout.png` `summary.txt` を開くボタンを追加する。
- [x] 成果物パスを JSON ファイル位置から相対解決する。
- [x] ファイル欠落時に状態表示を更新する。

## フェーズ 5: 使い勝手の調整

- [ ] 小さいウィンドウでも読みやすいレイアウトを保つ。
- [ ] 余白とパディングの一貫性を整える。
- [ ] 将来の国際化に備えつつ、当面は日本語ラベルを正本とする。

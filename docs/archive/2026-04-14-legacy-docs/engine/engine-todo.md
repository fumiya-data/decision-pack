# エンジン TODO

## 目的

- Lean仕様と整合した、実運用可能なシミュレーションエンジンを実装する。

## フェーズ 1: CSV 入出力とバリデーション

- [ ] CSVパーサ依存を追加する（`csv`, `serde`, `chrono` など）。
- 代替: 現時点は「report-first」方針で、CSV読込より先にJSONスキーマ契約と可視化経路を先行実装したため保留。
- [ ] `items`, `sales_daily`, `inventory_daily`, `staff`, `cashflow_daily` の入力構造体を定義する。
- [ ] スキーマ検証を実装する（必須列、型、キー重複）。
- [ ] 業務検証を実装する（数量非負、日付妥当性、既知の `item_id`）。
- [ ] 行/列情報つきのエラーレポートを実装する。

## フェーズ 2: 決定論シミュレーション

- [x] `InventoryOneDay` の意味と一致する在庫日次更新を実装する。
- [x] `CashOneDay` の意味と一致する資金日次更新を実装する。
- [ ] 欠品と保存則のチェックを実行時アサーションとして実装する。
- 代替: 現在は `inv_conservation_holds` / `cash_additive_holds` とテストで保証し、panicベースの実行時アサーションは採用していない。
- [x] 資金・在庫の日次状態出力を生成する。

## フェーズ 3: 制約とアラート

- [ ] 制約チェックを追加する（資金閾値割れ、欠品率超過、発注上限超過）。
- [x] 重要度と初回発生日を持つアラートモデルを実装する。
- [x] サマリKPIを追加する（最小資金、欠品数量/率、DOH）。

## フェーズ 4: シナリオエンジン

- [ ] シナリオパラメータモデルを追加する（需要係数、リードタイム係数、コスト係数）。
- [ ] ベースライン対シナリオ実行を実装する。
- [ ] シナリオ差分レポートを追加する（資金差分、利益差分、リスク差分）。

## フェーズ 5: モンテカルロ

- [ ] 需要とリードタイムの確率分布モデルを追加する。
- [ ] N回シミュレーション実行と分位集計（P10/P50/P90）を実装する。
- 補足: 分位集計ヘルパー（P10/P50/P90）のみ先行実装済み。シナリオ反復実行本体は未実装。
- [ ] 資金ショート/欠品イベントの発生確率を算出する。

## フェーズ 6: レポート出力

- [x] UI向けの安定したJSONスキーマを定義する。
- [x] `report/json.rs` にレポート生成を実装する。
- [ ] 図表生成パイプライン向けのエクスポートを追加する。
- 代替: エクスポート責務は `reporting/python` 側CLI（JSON入力→PNG/TXT出力）で分離運用している。

## テストと品質

- [x] 在庫・資金の日次ロジックの単体テストを追加する。
- [ ] 固定CSVを使ったゴールデンテストを追加する。
- 代替: 現在は固定JSON（スキーマ準拠）を中心に検証しており、CSVゴールデンはCSV I/O実装フェーズで追加予定。
- [ ] シナリオ比較の回帰テストを追加する。
- [x] CIチェックを追加する（`cargo test` + サンプル実行）。
